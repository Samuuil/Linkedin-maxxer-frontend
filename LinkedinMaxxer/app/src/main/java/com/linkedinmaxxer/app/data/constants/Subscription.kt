package com.linkedinmaxxer.app.data.constants

object Subscription {
    const val ROOT = "subscriptions"
    const val SUGGESTIONS = "$ROOT/suggestions"
    const val PENDING_SUGGESTIONS = "$SUGGESTIONS/pending"
    const val RESPOND = "$SUGGESTIONS/respond"
    const val AUTO_COMMENT = "$ROOT/auto-comment"
}
